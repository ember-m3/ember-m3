import { module, test } from 'qunit';

module('unit/model', function() {
  // Replace this with your real tests.
  test('it works', function(assert) {
    assert.ok(true);
  });


  test('it appears to ember data as a model', function(assert) {

  });

  module('.unknownProperty', function() {
    test('it returns undefined for attributes not included in the schema', function(assert) {

    });

    test('it returns schema-transformed values', function(assert) {

    });

    test('it resolves id-matched values to external m3-models', function(assert) {

    });

    test('it resolves id-matched values to external DS.models', function(assert) {

    });

    test('it resolves nested-matched values as nested m3-models', function(assert) {

    });

    test('it resolves arrays of id-matched values', function(assert) {

    });

    test('it resolves arrays of nested-matched values', function(assert) {

    });

    test('it resolves heterogenous arrays of id-matched, nested-matched and unmatched values', function(assert) {

    });
  });
});

